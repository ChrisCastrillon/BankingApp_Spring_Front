import { ApplicationTemplate } from './application-template';

describe('ApplicationTemplate', () => {
  it('should create an instance', () => {
    expect(new ApplicationTemplate()).toBeTruthy();
  });
});
