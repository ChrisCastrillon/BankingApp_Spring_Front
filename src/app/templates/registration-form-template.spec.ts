import { RegistrationFormTemplate } from './registration-form-template';

describe('RegistrationFormTemplate', () => {
  it('should create an instance', () => {
    expect(new RegistrationFormTemplate()).toBeTruthy();
  });
});
